# CLAUDE.md

**Strictly follow the rules in [./AGENTS.md](./AGENTS.md).**

That file is the single source of truth for this repo: build/test/lint commands,
the three lanes of work, which `docs/` file to load for a given task, and the
enforced hooks.

Do not duplicate rules here. If a rule needs to change, change `AGENTS.md`.
